# Diplom_2

# API Тестирование ручек API для Stellar Burgers.

##  🍔 Описание проекта

Этот проект предназначен для автоматизации тестирования API Stellar Burgers. 

Тесты охватывают создание пользователя, авторизацию, изменение данных пользователя, создание заказов и получение списка заказов конкретного пользователя. 

Проект использует `pytest` для запуска тестов и `allure` для создания отчетов о тестировании.

Ссылка на документацию API:

```
https://code.s3.yandex.net/qa-automation-engineer/python-full/diploma/api-documentation.pdf?etag=3403196b527ca03259bfd0cb41163a89
```

## 💽 Установка

### Установка зависимостей

1. Клонируйте репозиторий:
2. Установите необходимые зависимости:

    ```bash
    pip install -r requirements.txt
    ```

## ✏️ Структура проекта

- `api/` - Пакет с описанием методов для тестов.
- `tests/` - Пакет с тестами.
- `helpers/` - Пакет вспомогательными функциями и константами.
- `conftest.py` - Конфигурационный файл для pytest.
- `README.md` - Описание проекта.
- `requirements.txt`- Файл с внешними зависимостями.

## 🚀 Запуск тестов

Для запуска тестов используйте команду:

```bash
pytest --alluredir=allure-results
```

## 📊 Генерация Allure отчета

Для генерации отчета используйте команду:

```bash
allure serve allure-results
```
